# 추가 의존성 (필요시 사용)
# 기본 의존성 포함
-r requirements.txt

# 추가 도구들
gunicorn==21.2.0
psycopg2-binary==2.9.9  # PostgreSQL 지원 (필요시)

# 모니터링 (필요시)
prometheus-client==0.19.0
structlog==23.2.0

# 보안 (필요시)
cryptography==41.0.7
python-jose[cryptography]==3.3.0
